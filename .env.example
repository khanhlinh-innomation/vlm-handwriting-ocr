# Copy to .env only for local use. Never commit populated secrets.
VLM_DATA_ROOT=/workspace/data
VLM_RAW_ROOT=/workspace/data/uit-hwdb
VLM_MANIFEST_ROOT=/workspace/data/uit-hwdb-manifest
VLM_CHECKPOINT_ROOT=/workspace/checkpoints
VLM_OUTPUT_ROOT=/workspace/outputs
VLM_LOG_ROOT=/workspace/logs
HF_HOME=/workspace/.hf_home

KAGGLE_API_TOKEN=
HF_TOKEN=
